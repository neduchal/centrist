from distutils.core import setup

setup(
        name='centristLibrary',
        version='${PACKAGE_VERSION}',
        package_dir={ '': '${PROJECT_BINARY_DIR}'+"/CentristLibrary/python/" },
        packages=['centrist'],
        py_modules=['centrist2d']
    )
